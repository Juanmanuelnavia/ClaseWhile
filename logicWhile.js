var numeroEntrada=0;
var resultado=0;
var control=0;
numeroEntrada = parseInt(prompt("ingrese el numero que desea calcular"));
while (numeroEntrada>=control) {
    resultado=resultado+control;
    control=control+1;
    
}

alert("la suma de los primeros " +numeroEntrada + " es igual a " + resultado);